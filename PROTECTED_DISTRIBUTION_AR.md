# توزيع الأفق المحمي — التجريبي وPlus

## القرار النهائي
- تطبيق أندرويد واحد من ناحية الحزمة الأصلية: `com.alofok.trial`.
- النسخة التجريبية ونسخة Plus تستخدمان نفس الحزمة ونفس مفتاح التوقيع حتى يمكن تحويل التجريبي إلى Plus بواسطة تحديث فوق التطبيق نفسه من دون حذف البيانات.
- غير المشترك يحصل على قناة تحديث `trial` فقط.
- بعد تأكيد دفع Zain Cash وتغيير الاستحقاق إلى `plus`، زر «البحث عن تحديث» يفحص الترخيص ويحوّل المستخدم إلى قناة تحديث Plus.
- ملف APK وحده لا يمنح ترخيصًا. كل تثبيت يحتاج تفعيلًا رسميًا مرتبطًا بمعرّف تثبيت الجهاز.
- نسخة Plus لا تفتح إذا لم يرجع خادم الترخيص باستحقاق `plus` صالح.

## حماية النسخ إلى هاتف آخر
Android لا يوفر طريقة موثوقة للتطبيق لمعرفة أن ملف APK جاء فعليًا من صفحة ويب بعينها بعد أن أصبح الملف على الجهاز. لذلك الحماية المعتمدة هي «التفعيل الرسمي المرتبط بالجهاز» بدل الاعتماد على سرية ملف APK.

عند أول تشغيل للإصدار العام المحمي:
1. ينشئ التطبيق `installId` جديدًا لذلك التثبيت.
2. إذا لم يوجد ترخيص صالح، تظهر شاشة «هذه النسخة تحتاج تفعيلًا رسميًا» ولا تفتح واجهة التطبيق.
3. زر «التفعيل من المصدر الرسمي» يفتح صفحة Wissam Digital الرسمية مع `installId`.
4. الصفحة الرسمية تصدر رمز تفعيل قصير العمر وأحادي الاستخدام، ثم تعيد المستخدم إلى التطبيق عبر الرابط `alofok://activate?token=...`.
5. خادم الترخيص يربط الترخيص بالـ `installId` ويعيد `licenseToken`.
6. إذا نُسخ APK إلى هاتف ثانٍ، الهاتف الثاني ينشئ `installId` مختلفًا ولا يملك الترخيص الأول، لذلك يبقى التطبيق مقفولًا حتى يمر بالتفعيل الرسمي.

## واجهة خادم الترخيص المطلوبة
### POST `/v1/install/activate`
مدخلات العميل:
- `activationToken`
- `installId`
- `packageId`
- `appVersion`
- `appVariant`

الاستجابة الناجحة:
```json
{
  "valid": true,
  "tier": "trial",
  "licenseToken": "opaque-server-token",
  "expiresAt": null
}
```

### POST `/v1/license/verify`
مدخلات العميل:
- `licenseToken`
- `installId`
- `packageId`
- `appVersion`
- `appVariant`

الاستجابة:
```json
{
  "valid": true,
  "tier": "trial",
  "expiresAt": null
}
```
أو للمشترك:
```json
{
  "valid": true,
  "tier": "plus",
  "expiresAt": "2027-09-10T00:00:00Z"
}
```

## ربط Zain Cash
بعد تأكيد الدفع من جهة الخادم فقط، يتغير استحقاق التثبيت/الحساب من `trial` إلى `plus`. لا يوضع أي سر خاص بزين كاش داخل APK. بعدها يفتح المستخدم «البحث عن تحديث»، فيفحص التطبيق الاستحقاق، وإذا كان `plus` يستخدم `update-plus.json`، وإلا يستخدم `update-trial.json`.

## متغيرات الإصدار العام
يجب ضبطها قبل بناء النسخة العامة:
- `EXPO_PUBLIC_LICENSE_API_URL` عنوان خادم الترخيص.
- `EXPO_PUBLIC_OFFICIAL_PORTAL_URL` صفحة التفعيل الرسمية في Wissam Digital.
- `EXPO_PUBLIC_LICENSE_ENFORCEMENT=required` لمنع تشغيل أي تثبيت غير مفعل.

أثناء التطوير فقط، إذا لم تُضبط خدمة الترخيص ولم تكن `EXPO_PUBLIC_LICENSE_ENFORCEMENT=required`، يسمح التطبيق بالتشغيل حتى لا تتعطل معاينات التطوير.

## التوقيع
يجب أن تستخدم كل إصدارات التجريبي وPlus والتحديثات المستقبلية مفتاح توقيع Android إنتاجي واحد محفوظ بسرية. لا يجوز نشر المفتاح داخل المستودع.

## ملاحظة أمنية
لا توجد حماية برمجية تجعل تطبيق Android «مستحيل الاختراق» 100%. الهدف هنا أن نسخ APK أو إرساله إلى هاتف آخر لا يكفي لتشغيله، وأن تبقى صلاحية Plus بيد الخادم لا بمتغير محلي داخل التطبيق.
