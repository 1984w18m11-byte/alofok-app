# تقرير تدقيق الأفق — 13 سبتمبر 2026

## الحالة
- الفرع المستخدم للتدقيق: `audit-no-build-2026-09-13`.
- لم يتم بناء APK أو AAB، ولم يتم تشغيل Expo prebuild أو Gradle أو EAS build.
- لم يتم دمج هذه الإصلاحات إلى `main` بعد.
- الهدف من هذا الفرع: تثبيت الإصلاحات وفهرسة النواقص قبل أي إصدار جديد.

## إصلاحات نُفذت على فرع التدقيق
1. إصلاح خطأ `SCREEN` الذي بقي بعد حذف أطلس الثيمات وكان يمكن أن يسبب ReferenceError عند تحميل الواجهة.
2. إصلاح استعادة الموقع: القيم الفارغة لم تعد تتحول إلى الإحداثيات `0,0` بعد إعادة تشغيل التطبيق.
3. النسخة التجريبية تستخدم ثيم رمضان ثابتًا واحدًا ولا تعرض نظام تبديل الثيمات.
4. Plus أصبحت تملك منطق تبديل تلقائي يدخل فيه وقت اليوم والتاريخ ويوم الأسبوع والشهر القمري والفصل والسنة والمناسبات الزمنية المحددة، مع بقاء الاختيار اليدوي.
5. توحيد المساحة/التخطيط العلوي في الواجهة الرئيسية بين Trial وPlus، والفرق يبقى بالمزايا والثيمات.
6. إزالة التكرار الفعلي في أصول الثيمات: فُرض اختلاف صفحة المصدر المرخص واختلاف SHA-256 لكل ملف ثيم.
7. تحصين سكربت ترحيل الثيمات حتى لا يعيد كتابة `App.js` أو يرجع منطق الأطلس/التبديل القديم عند تشغيله لاحقًا.
8. تصحيح رسالة فحص التحديث: مشاكل الترخيص لم تعد تظهر تلقائيًا وكأنها انقطاع إنترنت.
9. تقوية Plus: عند غياب خدمة الترخيص لا تتفعل Plus تلقائيًا؛ التجاوز أصبح تجريبيًا وصريحًا فقط.
10. إغلاق نفس التجاوز في مسار التفعيل عبر الرابط العميق.
11. جدولة تنبيهات الصلاة مسبقًا بدل يوم واحد فقط: 14 يومًا على Android و7 أيام على iOS، وكذلك الإمساك والإفطار في رمضان.
12. إصلاح وراثة إعدادات Expo بين `app.json` و`app.config.js`؛ بعد الإصلاح نجح Expo Doctor، ونجح حل إعدادات Trial وPlus.
13. فحص `expo install --check` أكد أن تبعيات Expo الحالية محدثة ومتوافقة.
14. اختبارات المصدر `release-qa.js` و`check-translations.js` تمر بعد الإصلاحات الحالية.

## موانع إصدار يجب إنهاؤها قبل البناء النهائي
1. **الترخيص والدفع الحقيقي:** لا يوجد بعد خادم ترخيص/تفعيل إنتاجي مربوط فعليًا بالدفع. يلزم حسم Zain Cash/بوابة الدفع، إصدار رمز تفعيل، استعادة التفعيل بعد الفورمات أو تبديل الهاتف، وسياسة التجربة المجانية 7 أيام.
2. **الترجمة الكاملة:** قائمة اللغات كبيرة، لكن قسمًا مهمًا من النصوص يمر عبر `ui(ar,en)`، ولذلك اللغات غير العربية تحصل على الإنجليزية في نصوص كثيرة. فحص الترجمة الحالي لا يكتشف هذه الحالة لأنه يراجع مفاتيح `t(...)` فقط.
3. **نسب تراخيص صور الثيمات داخل التطبيق:** ملف `theme-sources.json` يحفظ المؤلف والرخصة وصفحة المصدر، لكن يجب توفير شاشة/قسم واضح للمستخدم لعرض نسب الصور التي تتطلب CC BY / CC BY-SA قبل النشر التجاري.
4. **التحديث والإصدار الجديد:** الإصدار المنشور 0.5.7 أقدم من إصلاحات اليوم. عند قرار البناء يجب رفع `version` و`versionCode` وتحديث manifests وروابط APK بعد نجاح الاختبار، وليس قبل ذلك.
5. **اختبار الأذان على أجهزة حقيقية:** الجدولة البرمجية تحسنت، لكن يلزم اختبار شاشة مقفلة، تطبيق مغلق، إعادة تشغيل الهاتف، وضع توفير البطارية، وأجهزة Android من شركات مختلفة. النافذة الحالية 14 يومًا على Android و7 أيام على iOS وليست جدولة لا نهائية.
6. **ملفات صوت الإشعارات وقت البناء:** الـWAV يتم توليدها/تنزيل مصادرها في workflow بدل حفظ النسخة النهائية كلها داخل المستودع. هذا يجعل الإصدار معتمدًا على توفر مصادر خارجية وقت البناء؛ يفضّل تثبيت آلية أكثر موثوقية قبل الإصدار النهائي.

## نواقص وظيفية/واجهة ما زالت موجودة
1. **كل اللغات:** يلزم تحويل جميع النصوص والتنبيهات والعناوين والإشعارات إلى نظام ترجمة حقيقي لكل لغة معتمدة، لا عربي/إنجليزي فقط.
2. **القائمة الرئيسية:** حاليًا الاختصارات المباشرة في القائمة أقل من التصميم المتفق عليه. المطلوب مراجعة وجود: الإعدادات، البحث عن تحديث، الاشتراك/Plus، الدعم، اللغات، حول، الخصوصية، بدون إعادة صفحات الصلاة والتقويم.
3. **اختيار المدن يدويًا:** ملف المدن اليدوي محدود وليس قائمة عالمية. GPS يعمل بالإحداثيات، لكن الاختيار اليدوي لكل مدن العالم غير مكتمل.
4. **منطقة GPS الزمنية:** موقع GPS يستخدم منطقة الجهاز الزمنية؛ إذا كان الجهاز لم يغيّر المنطقة الزمنية أثناء السفر فقد تتأثر الحسابات. يلزم حل timezone معتمد على الإحداثيات أو قاعدة مدن عالمية.
5. **ثيمات خلفية الموبايل:** الخيار الحالي لا يطبق الخلفية تلقائيًا فعليًا؛ على Android يفتح إعدادات الخلفية. إذا أريد التبديل التلقائي الحقيقي للهوم/القفل فيلزم تنفيذ Native مناسب وصلاحيات/قيود النظام.
6. **استعادة المشتريات:** الزر الحالي واجهة/شرح، وليس استعادة حقيقية مرتبطة بخادم أو متجر بعد.
7. **الدعم:** رقم/حساب الدعم التشغيلي ما زال يحتاج قيمة نهائية قبل النشر.
8. **الإعلان الأسبوعي:** ملف الإعلان معطل وفارغ افتراضيًا وهذا جيد عند عدم وجود إعلان، لكن يجب حسم هل المقصود ظهور واحد فقط في الأسبوع لكل مستخدم أم السماح بإعادة الظهور إلى أن يغلقه المستخدم.
9. **أصوات الأذان:** الأصوات الأربعة الحالية مرخصة، لكن قائمة الدول/الأصوات التي نريدها تجاريًا ما زالت غير مكتملة، خصوصًا أي أصوات إضافية للعراق/السعودية/مصر/تركيا أو خيارات أخرى؛ لا يضاف أي صوت بلا إثبات ترخيص واضح.
10. **صفحة اختيار الأذان:** القائمة مدمجة داخل الإعدادات؛ إذا أردنا الالتزام بالتصميم السابق لصفحة مستقلة، يلزم زر رجوع/إغلاق واضح.
11. **ترخيص الثيم المختار:** يفضل إظهار المؤلف والرخصة ورابط المصدر مباشرة من شاشة الثيمات، مع صفحة Credits كاملة.

## نواقص حسابية/بحثية يجب عدم إخفائها
1. محرك مواقيت الصلاة لا يملك بعد معالجة كاملة لحالات خطوط العرض العالية جدًا/الأيام التي لا يحدث فيها شروق أو غروب طبيعي.
2. نماذج القمر في `astronomy.js` تقريبية ومنخفضة الدقة مقارنة بمرجع ephemeris احترافي؛ لا ينبغي وصفها بأنها دقة JPL قبل المقارنة والتحقق.
3. التقويم القمري–الشمسي في التطبيق نموذج بحثي للأفق وليس تقويمًا شرعيًا رسميًا؛ يجب الإبقاء على هذا التوضيح في الواجهة والتوثيق.
4. الكسوف والخسوف موجودان كثيمات خاصة، لكن التفعيل التلقائي لهما حسب حدث فلكي حقيقي ليس مطبقًا بعد.

## تنظيف وصيانة قبل الإصدار
1. حذف/أرشفة ملفات النسخ الاحتياطية القديمة من المصدر بعد التأكد من عدم الحاجة إليها.
2. توحيد أسماء ملفات الأذان وإزالة النسخ المكررة غير المستخدمة بعد تثبيت workflow النهائي.
3. إزالة الحزم غير المستخدمة مثل أي اعتماد بقي من البوصلة بعد حذف ميزة البوصلة، إذا أكد فحص الاستيرادات أنه غير مستخدم.
4. تطوير اختبار الترجمة ليكشف نصوص `ui(ar,en)` غير المترجمة، وليس فقط مفاتيح `t(...)`.
5. إضافة فحص آلي لتراخيص الثيمات يمنع أي صورة بلا مصدر/رخصة/مؤلف ويمنع تكرار SHA والمصدر.

## شرط البناء القادم
لا يبدأ بناء Trial أو Plus إلا بعد مراجعة هذا التقرير وتحديد أي البنود نريد إنهاءها في الإصدار القادم. عند قرار البناء: ندمج الإصلاحات المختارة إلى `main`، نرفع رقم الإصدار وversionCode، نشغل QA كامل، ثم نبني النسختين ونختبر التثبيت والتحديث على الهاتف.
